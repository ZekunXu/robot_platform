def localProperties = new Properties()
def localPropertiesFile = rootProject.file('local.properties')
if (localPropertiesFile.exists()) {
    localPropertiesFile.withReader('UTF-8') { reader ->
        localProperties.load(reader)
    }
}

def flutterRoot = localProperties.getProperty('flutter.sdk')
if (flutterRoot == null) {
    throw new GradleException("Flutter SDK not found. Define location with flutter.sdk in the local.properties file.")
}

def flutterVersionCode = localProperties.getProperty('flutter.versionCode')
if (flutterVersionCode == null) {
    flutterVersionCode = '1'
}

def flutterVersionName = localProperties.getProperty('flutter.versionName')
if (flutterVersionName == null) {
    flutterVersionName = '1.0'
}
apply plugin: 'com.android.application'
apply plugin: 'kotlin-android'
apply from: "$flutterRoot/packages/flutter_tools/gradle/flutter.gradle"

android {
    compileSdkVersion 29

    sourceSets {
        main.java.srcDirs += 'src/main/kotlin'
    }

    lintOptions {
        disable 'InvalidPackage'
    }

    defaultConfig {
        // TODO: Specify your own unique Application ID (https://developer.android.com/studio/build/application-id.html).
        applicationId 'com.chenkeai.robot_platform'
        minSdkVersion 21
        targetSdkVersion 29
        versionCode flutterVersionCode.toInteger()
        versionName flutterVersionName
        ndk {
            abiFilters 'arm64-v8a'
        }
        manifestPlaceholders = [
                JPUSH_PKGNAME: "com.chenkeai.robot_platform",
                JPUSH_APPKEY: "bd54c9f26c93aac42d8c4d51",
                JPUSH_CHANNEL: "developer-default",
                XIAOMI_APPKEY : "5471901738172", // 小米平台注册的appkey
                XIAOMI_APPID : "2882303761519017172", // 小米平台注册的appid
        ]
    }

    // 高德地图中需要使用的到的签名信息
//    signingConfigs {
//        release {
//            keyAlias 'version1'
//            keyPassword 'xzk@0602'
//            storeFile file('../../certification/robot_platform.jks')
//            storePassword 'xzk@0602'
//        }
//    }

    buildTypes {
//        debug {
//            signingConfig signingConfigs.release
//        }
//        profile {
//            signingConfig signingConfigs.release
//        }
        release {
            // TODO: Add your own signing config for the release build.
            // Signing with the debug keys for now, so `flutter run --release` works.
            signingConfig signingConfigs.debug
        }
    }

    packagingOptions {
        exclude 'assets/location_map_gps_locked.png'
        exclude 'assets/location_map_gps_3d.png'
    }
}

flutter {
    source '../..'
}

dependencies {
    implementation fileTree(dir: 'libs', include: ['*.jar'])
    implementation "org.jetbrains.kotlin:kotlin-stdlib-jdk7:$kotlin_version"
    implementation 'cn.jiguang.sdk.plugin:fcm:3.9.1'
    implementation 'com.google.firebase:firebase-core:17.0.0'
    implementation 'com.google.firebase:firebase-messaging:20.0.0'
    implementation 'cn.jiguang.sdk:jpush:3.9.1'  // 此处以JPush 3.8.6 版本为例。
    implementation 'cn.jiguang.sdk:jcore:2.6.0'  // 此处以JCore 2.6.0 版本为例。
    implementation 'cn.jiguang.sdk.plugin:xiaomi:3.9.1'
    //高德2D地图
    implementation 'com.amap.api:map2d:latest.integration'
    //高德地图定位功能
    implementation 'com.amap.api:location:latest.integration'
    //高德地图搜索功能
    implementation 'com.amap.api:search:latest.integration'
    //高德地图导航功能
    implementation 'com.amap.api:navi-3dmap:latest.integration'

}

apply plugin: 'com.google.gms.google-services'
